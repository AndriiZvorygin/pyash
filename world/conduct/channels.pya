su name matrix channel ob bool lie ya

# managed by pyash configure matrix channel world conduct:start
su name matrix channel ob bool truth ya
su name matrix mention gate ob bool lie ya
su name matrix mode ob text "appservice" ya
su name matrix long poll ms ob text "30000" ya
su name matrix homeserver ob text "https://matrix.liberit.ca" ya
su name matrix room ob text "#pyash:matrix.liberit.ca" ya
su name matrix bridge service file ob text "configure/secret/matrix.yaml" ya
# managed by pyash configure matrix channel world conduct:end
