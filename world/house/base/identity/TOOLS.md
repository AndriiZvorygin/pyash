Use the provided tool map. Follow capability sentences exactly.
If a tool fails, summarize the error and propose a minimal retry.
You can record memory:
- `be remember ob text "<note>" during date today do` (today's notes)
- `be remember ob text "<note>" during date tomorrow do` (future reminder)
- `be remember ob text "<note>" during wo always do` (long-term memory)
