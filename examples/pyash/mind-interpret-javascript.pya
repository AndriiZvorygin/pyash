su name example meta be map def
su name required ob ve text "OLLAMA_HOST" ya
prah

exists su name helper be mind via state "qwen3-vl:8b-instruct" ya

su name script ob discourse "Return only JavaScript source that prints 6*7. No fences or extra text." for name helper to name text script be write do
ob name text script as wo javascript during num 0.5 be interpret do
ob name result be write do
