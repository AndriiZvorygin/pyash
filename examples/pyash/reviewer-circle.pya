from name ../../module/verifier_circle.pya to name verifier circle module be import do

su name author ob text input to name text output be ceremony def
ob text "Hello from author." to name text output be text do
su name output ret
prah

su name verifier ob text input to name text output be ceremony def
ob text "Looks good.\nPASS" to name text output be text do
su name output ret
prah

exists su name input ob text "Write a short greeting." be text ya

ob name text input for name text author by name text verifier atleast num 0.8 atmost num 3 to name text output be verifier circle module verifier trying do
ob name output be write do
to name text report out be reporter do
ob name report out be write do
