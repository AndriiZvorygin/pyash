su name example meta be map def
su name required ob ve text "PYA_MIND_TOOL" "OLLAMA_HOST" ya
prah

su name tools be map def
su name read be read from filename "path" become wo text can
prah

exists su name verifier be mind via state "qwen3-vl:8b-instruct" fromtext text "You must call tools to read documentation/specifications/13-cheat-sheet.md and documentation/specifications/14-index-map.md. Tool call shape: be read from filename <path> become wo text (no to case). If you cannot see tools, are confused about how to call them, or do not receive tool results, respond exactly: NEED_TOOL_HELP. After tool calls, respond with: (1) a clarity verdict, (2) 3 improvements, (3) a tiny Pyash program (no markdown fences) that downloads a page, reads it as text, and writes a summary file, (4) one-sentence explanation of why the program follows the cheat sheet. Output must be Pyash only and must use sentences, not other languages." ya
ob text "Verify the cheat sheet and index map now." for name verifier to name text verify-out with name tools be write do
ob name verify-out be write do
