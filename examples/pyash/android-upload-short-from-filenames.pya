ob ve filename text video filename text metadata be input ya
from filename "./modules/android_intro.pya" ob name android introductory home-page be import do

exists su name phone shorts dir ob text "/sdcard/Movies/Pyash/" be default ya

su name android upload short from filenames be ceremony def
  su name video source stage ob name video to name filename video source be filename do
  su name metadata source stage ob name metadata to name filename metadata source be filename do
  su name video source path stage ob name video source stage to name text video source path be text do
  su name metadata source path stage ob name metadata source stage to name text metadata source path be text do

  su name verify handle vyah start future be android verify do
  accordingto text "verify handle" during num 12000 vyah await be android do

  su name send video handle from filename of ob of video source stage to text of ob of phone shorts dir vyah start future be android send do
  accordingto text "send video handle" during num 20000 vyah await be android do

  su name send metadata handle from filename of ob of metadata source stage to text of ob of phone shorts dir vyah start future be android send do
  accordingto text "send metadata handle" during num 12000 vyah await be android do

  be android introductory home-page do

  su name youtube opening ob text "com.google.android.youtube" vyah start future be android begin do
  accordingto text "youtube opening" during num 12000 vyah await be android do

  su name shorts cross touch ob ve num 540 2220 vyah start future be android tap do
  accordingto text "shorts cross touch" during num 12000 vyah await be android do

  su name selection observe handle vyah start future be android observe do
  accordingto text "selection observe handle" during num 12000 vyah await be android do

  su name note stage ob text quoted.text.Upload prep complete.
Phone folder: /sdcard/Movies/Pyash/
From Short selection screen, choose the transferred video and paste title/description/tags from the transferred metadata text file..text.quoted be write do
prah

be android upload short from filenames do
