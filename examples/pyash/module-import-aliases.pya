from name bump tools to name first be import do
from name ./modules/bump_tools.pya to name second be import do

be first bump do
be second bump do

ob name first counter be write do
ob name second counter be write do
