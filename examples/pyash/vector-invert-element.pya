exists subj name switches obj ve text truth lie truth be vector ya

obj name switches via space num 2 be invert do
obj name switches via space num 2 be read to name state do

subj name state be pyash que
