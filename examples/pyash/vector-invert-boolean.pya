exists subj name switches obj ve bool truth lie truth be vector ya

obj name switches via space num 1 be invert do
obj name switches via space num 1 be read to name status do

subj name status be pyash que
